# Section 1 – Shell Foundations & Python Interoperability

This section I learned: 
- more about how to use the command line
- how to automate tasks with Python scripts
- how to set up a proper development environment using .venv

---

## Sentinel Script

The sentinel script checks whether a specific directory exists.

### How to run it:

bash
cd section1/task8
chmod +x sentinel
./sentinel

## Useful commands I learned (definitely more than 3!)

cd
ls
mkdir
touch
python3 -m venv .venv
source .venv/bin/activate
pip
python3 (general use)
mv
cp
ssh-keygen
cat
ssh
echo
git add / git commit -m / git push origin main
